	<Directory />
		Options FollowSymLinks
		AllowOverride None
	</Directory>
	<Directory /var/site/DOMAIN_NAME/www/>
		Options Indexes Includes FollowSymLinks ExecCGI MultiViews
		AllowOverride None
		Order allow,deny
		allow from all
	</Directory>

# phpPgAdmin
	Alias /phpPgAdmin /usr/share/phpPgAdmin

	<Location /phpPgAdmin>
		Options All
#		    Order deny,allow
#		    Deny from all
#		    Allow from 127.0.0.1
#		    Allow from ::1
		    # Allow from .example.com

	       	AuthUserFile /var/site/DOMAIN_NAME/conf/.htpasswd
       		AuthGroupFile /dev/null
       		AuthName "Please enter your ID and password"
       		AuthType Basic
       		require valid-user
	</Location>

#ScriptAlias /cgi-bin/ /usr/lib/cgi-bin/
#	<Directory "/usr/lib/cgi-bin">
#		AllowOverride None
#		Options +ExecCGI -MultiViews +SymLinksIfOwnerMatch
#		Order allow,deny
#		Allow from all
#	</Directory>

	ServerSignature On

   	DirectoryIndex index.html index.html.var index.htm index.shtml index.shtm index.php index.cgi index.hdml 

   	AccessFileName .htaccess

#
# The following lines prevent .htaccess and .htpasswd files from being
# viewed by Web clients.
#
	<Files ~ "^\.ht">
	    Order allow,deny
	    Deny from all
	</Files>

#
# TypesConfig describes where the mime.types file (or equivalent) is
# to be found.
#
	TypesConfig /etc/mime.types

	DefaultType text/plain
	HostnameLookups Off

	Alias /icons/ "/var/www/icons/"
	<Directory "/var/www/icons">
	    Options Indexes MultiViews
	    AllowOverride None
	    Order allow,deny
	    Allow from all
	</Directory>

#
# WebDAV module configuration section.
# 
	<IfModule mod_dav_fs.c>
	    # Location of the WebDAV lock database.
	    DAVLockDB /var/lib/dav/lockdb
	</IfModule>

# *Redirect
# Redirect permanent /foo http://www.example.com/bar

	IndexOptions FancyIndexing VersionSort NameWidth=* HTMLTable

